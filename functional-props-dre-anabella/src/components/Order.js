import React from "react"

const Order = (props) => {
    <>
    <div className="order">
        <h2>Order</h2>
        
          <button onClick={() => props.addToOrder(index)}>order</button>
    </div>
    </>
}
export default Order